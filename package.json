{
  "name": "playwright-yaml-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "start:mcp": "set HEADLESS=false && npx @playwright/mcp@latest --port=3000 --user-data-dir gemini-playwright --save-trace --output-dir test-artifacts --browser chromium"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@playwright/test": "^1.54.1",
    "playwright": "^1.54.1"
  }
}
